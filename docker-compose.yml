services:
  authmcp-gateway:
    build: .
    container_name: authmcp-gateway
    ports:
      - "9105:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
